# Supabase (client)
# Public URL and anon key are used by client-side code. Replace these with your project's values.
# Supabase client (public)
# Public URL and anon key are used by client-side code. Replace these with your project's values.
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your_anon_key_here"


# Supabase (server)
# Server-only service role key. DO NOT commit real service keys. Set this in your deployment or in `.env.local`.
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_here"


# OpenAI
# Server-side OpenAI API key. Set in `.env.local` or your hosting environment - do not commit.
OPENAI_API_KEY="your_openai_api_key_here"